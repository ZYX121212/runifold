# Baselines

No measured baseline is committed yet. Pin a real Git commit and tool/model version
for each run. Compare the same task, fixture, limits and grading rubric across
conditions: baseline, instructions+recipes, manifest, diagnostics, affected checks.
Store JSON evidence under target/ai-evals/ or an explicitly selected artifact path.
Never report an unrun task as passing or a fabricated tool compatibility score.
